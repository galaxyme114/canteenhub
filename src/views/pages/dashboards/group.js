import {
  Card, CardHeader, CardBody, CardTitle, CardText, CardLink,
} from 'reactstrap';

const Home = () => (
  <div>
    <Card>
      <CardHeader>
        <CardTitle>School Dashboard</CardTitle>
      </CardHeader>
    </Card>

  </div>
);

export default Home;
